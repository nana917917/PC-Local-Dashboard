# PC Local Dashboard v0.12 仕様

## 方針

- 常駐するのは既存のWattSeal計測だけ。表示サーバー・PC状態取得・容量スキャンは必要な時だけ動かす。
- 表示サーバーは既定で`127.0.0.1`のみ。同一LAN閲覧は設定で明示的に有効化し、LAN側は読み取り専用とする。
- 外部サーバー、アカウント、クラウド保存を使わない。記録は`%LOCALAPPDATA%\PCPowerHistory`の中だけに置く。
- 取得できないセンサーやDB列があっても、他の機能は継続する。取得できない値は0ではなく「未取得」「未対応」と表示する。
- 既存の`power_monitoring.db`（WattSeal v1.0.2系・旧形式の両方）と`config.json`を更新セットアップで保持する。
- 数値の意味（実測／推定／欠損／0W／1時間平均）を画面と出力の両方で区別する。
- 機能を増やすより「見やすい・誤解しにくい・深掘りできる・軽い・壊れにくい・直しやすい」を優先する。

## 画面

上部タブは`概要`、`履歴・料金`、`セッション`、`PC状態`、`容量`の5つ。上部の期間バーは全画面で共通。

### 期間と粒度

| 期間 | 集計の粒度 |
| --- | --- |
| 今日・昨日（4時間以内の表示） | 1分 |
| 8時間以内 | 5分 |
| 10日以内（7日・30日以外の短期） | 1時間 |
| 1年以内（30日・90日・全期間など） | 1日 |
| 長期間（1200日超） | 1か月 |

バケット数が1600を超える場合は自動で粗い粒度へ切り替える。期間の長さが変われば、グラフ・比較・表も同時に切り替わる。

### 概要

現在の推定電力（15秒より古い記録・1時間平均の記録は現在値として扱わない）、選択期間の推定料金・電力量・平均/最大W・記録された使用時間、いまの負荷状態（アイドル／通常／高負荷）、前日・同期間との比較、今月の着地予測、もっとも電力を使った時間、直近の電力グラフ、主な要因（部品・アプリの推定）を表示する。長い一覧は置かない。

### 履歴・料金

電力(W)・電力量・料金・累積料金を切り替えて表示する。ホバー／タップで日時・平均W・最大W・最小W・kWh・推定料金・データ品質を確認できる。比較は直前の同期間と前年同期間を選べる。

数値の確認表には、区間・平均W・最大W・kWh・推定料金・使用時間・データ品質・前期間との差を表示する。グラフと表は同じ集計結果を使い、値が食い違わないようにする。

気づきとして、電力が急に上がった時間、2時間以上続いたアイドル、5分以上の記録のない時間、24時間の平均パターン、日別ヒートマップ、部品・アプリ別の内訳（推定）を表示する。

### セッション

1秒記録が残っている期間から、起動していた区間（10分以上の空白で区切る）を復元し、開始・終了・継続時間・平均W・最大W・推定kWh・推定料金・アイドル時間・主なアプリ（推定）・記録状態を表示する。1時間平均しかない期間は分解能が粗いため、その旨を表示する。

### PC状態

CPU・GPU・メモリ・ストレージ・ネットワーク・稼働時間・温度・ファン・クロック・ドライブ状態を表示する。各項目に取得状態（取得済み／未取得／未対応／エラー／古い値）と取得元を付ける。取得できない項目は0にしない。使用率の履歴はWattSeal DBに列がある場合のみ表示する。

### 容量

既存の容量マップを埋め込みで表示し、上部にスキャン状態・最終スキャン・保存サイズを表示する。スキャンは本人操作のみ。実ファイルは削除しない。

## データと集計

- 記録なし（欠損）は`null`として扱い、0円・0kWhとして加算しない。
- 0Wとして記録された区間は`quality: zero`として区別する。
- 1時間平均の記録は`quality: rollup`とし、使用時間は概算として扱う。
- 比較は、対象期間の記録率が80%未満・記録なし・消費0の場合は行わず、理由を表示する。期間の長さが2%以上違う場合は「単純比較」とする。
- アプリ別の配分は、記録行数が多いときは間引いて計算し、その旨を表示する。未分類を残し、合計がPC全体と一致するようには表示しない。
- 重い集計（部品別・アプリ別・セッション）は120秒の短期キャッシュを使い、画面の定期更新でPCに負担をかけない。

## 出力と操作

- CSV: 区間・平均W・最大W・最小W・kWh・推定料金・使用時間・データ品質・欠損時間・データ点数・前期間との差を出力し、末尾に推定値であることと計算条件を付ける。
- JSON: 同じ内容を構造化して出力する。
- DBバックアップ: 読み取り専用接続から`VACUUM INTO`で一貫性のある複製を作り、ダウンロード後に一時ファイルを削除する。
- 指定期間の削除・全履歴の初期化: 標準の保存場所にあるDBのみを対象にし、WattSealを停止してから実行し、確認文字列と確認ダイアログを必須にする。完了後は自動記録を再開する。
- 設定の初期化は記録データを消さない別操作とする。

## ログ

- 通常ログ（`logs/app.log`）: 人が読む要約。長い標準出力・個人名・PC名・絶対パス・秘密情報・スタックトレースを出さない。
- 詳細ログ（`logs/debug.log`）: JSON形式で、エラー種別・発生箇所・HTTPステータス・センサー取得結果・例外の技術情報を記録する。
- 512KBを超えたら1世代だけローテーションする。

## セキュリティ

- 待ち受けは既定で`127.0.0.1`。LAN公開を有効にした場合のみ`0.0.0.0`で待ち受ける。
- 接続を許可するのは、このPCのネットワークインターフェース（同じサブネット）から求めたネットワークに含まれる接続元だけとする。広いCIDR（10/8、100.64/10など）をまとめて許可しない。PC自身がそのサブネットにいる場合だけ、そのサブネット（例: `100.64.1.0/24`）が許可対象になる。
- 仮想アダプター（WSL・Hyper-V・Docker・VPN等）は既定で許可しない。別サブネットからの接続が必要な場合のみ、設定`lanAllowedNetworks`にCIDRを明示的に追加する。
- LAN側は読み取り専用。書き込みAPI（設定・削除・再起動・DBバックアップ）は拒否し、画面でも操作できないようにする。
- LAN側へ返す応答からは、DB・ログの絶対パス、PC名（hostname）、ドライブのボリューム名を除外する。
- 容量マップ（フォルダ名・パスを扱う）は`127.0.0.1`でのみ起動し、LAN側からは概要のみ表示する。
- 書き込みAPIは同一Originのみ受け付ける。`GET`以外のメソッドを書き込みとして扱う。
- 静的ファイル配信は公開ディレクトリの外へ出られないようにし、リクエスト本文は16KBを超えると413で拒否する。
- スマホは「ホーム画面に追加」による補助的なショートカットのみとし、Service Workerによるオフライン動作は提供しない。

## 配布

ZIPを展開して`SETUP.cmd`を1回実行する。実体はLocalAppDataへ配置し、デスクトップショートカット、自動記録、Windowsのアンインストール情報を作成する。強制更新・自動更新は実装しない。

## 将来の候補（今回は実装しない）

- 壁コンセント実測値の記録と、WattSeal値との差分・校正履歴の管理（校正係数だけは実装済み）
- アイドル時オフセットの自動調整
- 閾値を超えたときのWindows通知（現在は画面表示のみ）
- Electron化、クラウド同期、外部サーバーへの履歴送信、複雑なユーザー認証
- ファン・CPU温度などの追加センサー（対応ツールの同梱が必要になるため）
