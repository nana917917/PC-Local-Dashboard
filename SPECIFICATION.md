# PC Local Dashboard v0.10 仕様

## 方針

- 普段の常駐は既存のWattSeal計測だけにする。
- 表示・PC状態取得・容量スキャンは必要な時だけ動かす。
- 外部サーバー、アカウント、クラウド保存を使わない。
- 取得できないセンサーやDB列があっても、他の機能は継続する。
- 既存の`power_monitoring.db`と`config.json`を更新セットアップで保持する。

## 画面

上部タブは`電力`、`容量`、`PC状態`の3つ。電力画面の従来構成を基準にし、主要な結論を最上部へ追加する。

### 電力

現在W、選択期間の料金・kWh・時間、リアルタイム折れ線、履歴棒グラフ、累積料金折れ線、部品・用途・アプリ内訳、起動セッション、CSVを提供する。リアルタイム範囲は5／15／60分。集計範囲は今回／今日／7日／今月／30日／90日／1年／全期間。最新サンプルから最大消費部品・アプリと直近ピークを表示する。直前の同期間比較、最大料金時間、1時間継続時料金を表示する。比較期間の記録が十分な場合、累積料金へ直前期間を重ねる。時間軸は水平かつ画面幅に応じて間引き、ホバーまたはタッチで詳細値を表示する。

### 容量

別常駐処理は持たず、タブを初めて開いた時だけ容量画面サーバーを起動する。スキャンは本人操作のみ。ツリーマップ、用途分類、大容量一覧、同一ルートの前回差分を表示する。実ファイルは削除しない。

### PC状態

タブ表示中だけ約2秒間隔で現在値を取得する。CPU・GPU・RAM、GPU温度、稼働時間、ドライブ空き、WattSeal状態を表示する。WattSeal DBが対応する場合、直近15分／今日の使用率履歴も表示する。

## データ

自動削除はしない。設定画面からDB容量と記録期間を確認し、CSV保存、容量キャッシュ削除、電力DB初期化、設定初期化を本人操作で行える。電力DB削除は二重確認と入力確認を行い、対象を標準DB名に限定する。

## 配布

ZIPを展開して`SETUP.cmd`を1回実行する。実体はLocalAppDataへ配置し、デスクトップショートカット、自動記録、Windowsのアンインストール情報を作成する。強制更新・自動更新は実装しない。
