{
  "name": "pc-local-dashboard",
  "version": "0.12.1",
  "private": true,
  "description": "Local Windows dashboard for PC power, storage usage, and system status",
  "license": "MIT",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node app/server.js",
    "check": "node tests/check-static.js",
    "test": "npm run check && node --test tests/api.test.js tests/unit.test.js"
  }
}
