# 既定: テキストはLF、バイナリはそのまま
* text=auto eol=lf
*.png binary
*.ico binary
*.zip binary

# Windows用スクリプトはCRLFのまま扱う。
# （gitattributesは後ろの行が優先されるため、既定より後に書く）
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
*.vbs text eol=crlf
