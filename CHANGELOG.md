# Changelog

## 0.12.1 - 2026-09-14

- 同じ1時間に「1秒記録」と「1時間平均」が併存する人工DBで二重計上を確認し、**1秒記録を優先する採用規則**を実装（電力量・使用時間の重複を解消。採用した時間帯数を`deduplicatedHours`として表示）
- 1時間平均しかない区間から瞬間の最大・最小を作らないよう修正。期間内に混在する場合は「最大Wは1秒記録のある区間のみ」と画面に明記
- 時間加重平均（推定kWh×3600÷記録秒数）、期間端の切り取り、固定Wを記録がある時間にだけ加算することをテストで固定
- 期間削除・バックアップ・復元・失敗時の挙動をテスト用DBで検証（書き込み中は削除せず記録を保持、削除後も`integrity_check`がok、孤児行なし、バックアップから復元可能）
- 削除系APIが標準の保存場所以外では実行されないことをテストで確認（テスト用DBを誤って削除しない）
- 日本語・空白を含むパスとPowerShell 5.1で、インストーラー相当の処理（コピー・設定保持・ハッシュ・Unicode読み書き・不要ファイル整理）とVBS起動処理が動くことを確認
- 軽量性を実測（アイドル時 約0.5%CPU/約52MB、1タブ表示中 約1.7%、2タブ 約2.6%。PC状態タブのセンサー取得は子プロセス3個で1回分のみ）
- 自動終了（無アクセス時）とサーバー維持モードの切り替え、ダッシュボード停止中もWattSealの記録が継続することを実測
- 動作確認用に`PC_POWER_IDLE_MS`（自動終了までの時間を上書き）を追加（既定は従来どおり約10分）

## 0.12.0 - 2026-09-12

- LAN接続の許可範囲を「このPCと同じサブネット」だけに限定（従来の10/8・100.64/10などの広い許可を廃止）。追加のサブネットは設定で明示的に指定
- LAN側へ返す内容から、DB・ログの絶対パス、PC名、ドライブのボリューム名を除外
- 容量マップは127.0.0.1でのみ起動し、スマホからは概要のみ表示（フォルダ名・パスをLANへ出さない）
- 保存済みの容量スキャン結果が無い場合、`/api/result`は404ではなく200＋状態を返すようにし、不要なブラウザーエラーを解消
- Service Workerを削除し、PWAは「ホーム画面に追加」用の補助（オフライン動作なし・HTTPSでないLANではインストールできない場合がある）と明記
- インストーラーが旧バージョンの不要ファイルを整理するように変更。アンインストーラーは自分自身のインストール先のWattSealだけを停止
- 配布ZIPを作る`tools/build-release.js`を追加（個人データの混入チェック付き）
- データ取得状態（記録中／最終取得からの経過／古い／未接続／一部欠損／推定値／比較不能）を画面上部に明示
- 記録なし（欠損）・0Wとして記録・1時間平均の記録・未対応センサーを区別し、欠損を0として扱わないよう集計を変更
- 期間選択とグラフの粒度を連動（分→時間→日→月を自動選択）。今日・昨日・7日・30日・今月・先月・今年・1年・全期間・カスタム期間に対応
- 前期間・前年同期間の比較を追加し、比較対象の記録が不足しているときは増減率を表示しないよう変更
- グラフのホバー詳細（平均W・最大W・最小W・kWh・推定料金・品質）と、グラフ下の数値確認表を追加
- 日別ヒートマップ、24時間の平均パターン、電力の急上昇、長時間アイドル、記録のない時間の抽出を追加
- セッション分析を改善（継続時間・平均/最大W・推定kWh・推定料金・主なアプリ・アイドル時間・記録状態）
- アプリ分類の手動設定と分類の追加（ゲーム／ブラウザー／動画・音楽／開発／仕事・制作／ファイル処理／通話・連絡／Windows・常駐／その他）
- 設定に「実測との校正係数」「表示単位」「更新間隔」「通知しきい値」「保持期間の目安」「サーバー維持」を追加
- CSV/JSON出力、DBバックアップ、指定期間だけの削除を追加（全履歴の初期化・設定の初期化とは別操作）
- LAN閲覧を読み取り専用化（設定変更・削除・再起動・バックアップはPC内からのみ）。スマホ用URLとサーバー稼働状態を画面に表示
- 表示サーバーの自動終了をREADMEの記載どおり約10分に統一し、任意のサーバー維持モードを追加
- 通常ログ（app.log）と詳細ログ（debug.log）を分離し、ユーザー名・PC名・絶対パスを置換して保存
- 画面を5タブ（概要／履歴・料金／セッション／PC状態／容量）へ再構成し、スマホ幅では期間タブを折り返して横スクロールをなくした
- スマホで「ホーム画面に追加」しやすいようにマニフェスト（名前・アイコン）を追加（Service Workerは登録せず、オフライン動作は提供しない）
- テストを刷新（空DB・1件・1日・7日・30日・400日・旧形式DB・欠損・0W・古い値・境界・LAN・エクスポート・ログ）

## 0.11.0 - 2026-09-04

- 初見向けトップ画面と展開式の詳細画面へ再構成
- 同一Wi-Fi内のスマホ閲覧を設定式で追加
- 古い電力値を現在値として表示しないよう修正
- 使用率履歴の欠損値を0として描画しないよう修正
- 複数GPUの使用率・温度・メモリ・電力を集約
- 取得可能なドライブ健康状態を表示
- WattSeal v1.0.2の固定SHA-256検証を追加
- セットアップログを人が読める要約形式へ変更
- セットアップ・履歴初期化時のWattSeal停止対象を自アプリの実体へ限定
- ローカルAPIの異なるOriginからのPOSTを拒否

## 0.10.0 - 2026-08-14

- 90日、1年の集計範囲を追加
- 選択期間先頭から積み上げる累積料金折れ線を追加
- 時間別料金グラフの斜めラベルを水平・自動間引きへ変更
- グラフのホバーツールチップと縦カーソルを追加
- 直前の同期間との料金比較を追加
- 累積料金へ直前期間の折れ線を重ねて比較
- 期間内で最も料金が高かった時間／日を追加
- 現在Wを1時間続けた場合の推定料金を追加
- 任意の月間料金目安と、予測の超過／余裕表示を追加
- 7日以上の期間を選んだ時は累積料金へ自動切替
- 長期グラフでも日付を読みやすい表示へ変更

## 0.9.0 - 2026-08-08

- 電力、容量、PC状態を1つのローカルダッシュボードへ統合
- リアルタイムW、用途・アプリ内訳、容量差分、PC状態、データ管理を追加
